+ 1. check how retrieving of IDs works
+ 2. define task/command in StoryItem to retrieve its story
+ 3. wire everything together 

+ write decoder for story's json
+ test decoder make sure it works correctly 
- refactor current implementation of Infinite Scroll, in particular
  how 'Scroll' event is handled in App's update 
- Add view for story that display loading state as before
