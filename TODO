Implement navigation
 - introduce Parent/Child message in Sidebar
 - when click happens of link route message to parent that will change part of state responsible 
   for navigation
 - based on this change list of stories

HN Reader:
  + implement preloading for Stories
  - Add constant update functionality to HN Reader
  - add LocalStorage support for HN-Reader
  + Add Html.lazy to HN-Reader
  - reimplement my Routing strategy
  - refactor 'LoadMoreStories' into a command
  - Learn about other approaches to Routing 
  - add same functionality as a feedly does for its items

!!! I think I can to check whether story exists in cache before forcing it to load from server